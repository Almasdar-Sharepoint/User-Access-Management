name: SPFx DEV CI/CD

on:
  push:
    branches:
      - Dev
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    environment: DEV
    
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16.20.2'
          cache: 'npm'

      - name: Verify DEV Client ID
        shell: bash
        run: |
          if [ -z "$CLIENT_ID" ]; then
            echo "CLIENT_ID is NOT available"
            exit 1
          else
            echo "CLIENT_ID is available"
          fi
        env:
          CLIENT_ID: ${{ secrets.CLIENT_ID }}

      - name: Generate DEV config
        shell: bash
        run: |
          cat > src/webparts/userAccessManagement/config.ts <<EOF
          export const appConfig = {
            clientId: "${CLIENT_ID}"
          };
          EOF
        env:
          CLIENT_ID: ${{ secrets.CLIENT_ID }}

      - name: Verify generated config
        shell: bash
        run: |
          if grep -q 'clientId: ""' src/webparts/userAccessManagement/config.ts; then
            echo "ERROR: Client ID was not generated"
            exit 1
          else
            echo "DEV config contains a Client ID"
          fi

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Verify Azure Login
        run: |
         az account show

      - name: Verify SharePoint App Catalog Access
        run: |
          az rest \
          --method GET \
           --url "https://graph.microsoft.com/v1.0/sites/acuvatehyd.sharepoint.com:/sites/Al-masdar"

      - name: Install dependencies
        run: npm ci

      - name: Build SPFx solution
        run: npx gulp bundle

      - name: Package SPFx solution
        run: npx gulp package-solution --ship

      - name: Upload SPFx package
        uses: actions/upload-artifact@v4
        with:
          name: user-access-management-dev
          path: solution/user-access-management.sppkg

